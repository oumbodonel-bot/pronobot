[build]
builder = "NIXPACKS"
nixPkgs = ["python311" "python311Packages.pip"]

[deploy]
startCommand = "pip install psycopg2-binary httpx python-telegram-bot python-dotenv && python bot.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
