[build]
builder = "NIXPACKS"
nixPkgs = ["zlib"]

[deploy]
startCommand = "source /opt/venv/bin/activate && python bot.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[cron]
command = "source /opt/venv/bin/activate && python generate_pronos.py"
